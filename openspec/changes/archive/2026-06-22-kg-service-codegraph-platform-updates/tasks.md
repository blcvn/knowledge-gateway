# Tasks

- [x] **T1** — Đánh giá bottleneck thực tế của core path trước khi thêm endpoint mới.
- [x] **T2** — Nếu cần, thêm `POST /v1/kg/write/nodes/bulk`.
- [x] **T3** — Nếu cần, thêm `POST /v1/kg/write/relationships/bulk`.
- [x] **T4** — Nếu cần cleanup nhanh, thêm `DELETE /v1/kg/write/nodes:by-external-ref-prefix`.
- [x] **T5** — Nếu cần raw graph-query ergonomics, thêm `POST /v1/kg/search/graph`.
- [x] **T6** — Verify semantic equivalence giữa core path và extension path.
