# ⚠️ DEPRECATED — memobase-ingestion

> This service has been consolidated into `memobase-pipeline` per SOL-001.
> Pipeline stages now execute as local function calls within `memobase-pipeline`.
> Proto definitions unchanged — backward compatible.
