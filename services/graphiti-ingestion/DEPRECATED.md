# ⚠️ DEPRECATED — graphiti-ingestion

> This service has been consolidated into `graphiti-pipeline` per SOL-001.
> Pipeline stages now execute as local function calls within `graphiti-pipeline`.
> Proto definitions unchanged — backward compatible.
