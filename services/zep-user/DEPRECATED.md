# ⚠️ DEPRECATED — zep-user

> This service has been consolidated into `zep-core` per SOL-001.
> Tightly coupled operations now execute as local function calls.
> Proto definitions unchanged — backward compatible.
