# ⚠️ DEPRECATED — sm-mcp

> This service has been consolidated into `vnp-gateway` per SOL-001.
> All gRPC service definitions are now registered in `vnp-gateway`.
> Proto definitions unchanged — backward compatible.
> This directory will be archived after Phase 4 completion.
