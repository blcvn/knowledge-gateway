# ⚠️ DEPRECATED — cognee-ingestion

> This service has been consolidated into `cognee-pipeline` per SOL-001.
> Pipeline stages now execute as local function calls within `cognee-pipeline`.
> Proto definitions unchanged — backward compatible.
