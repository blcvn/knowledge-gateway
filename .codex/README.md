# KG Service Codex Workspace

This directory mirrors the repo-local workflow helpers used by Codex.

- `commands/` contains short workflow entry points.
- `skills/` contains OpenSpec-oriented guidance for Codex.
- Keep `openspec/` as the source of truth for changes, tasks, and implementation order.

Use these helpers when a change needs discovery, proposal, implementation, or archive flow inside this repo.
