---
description: "Create a conventional commit from the current worktree"
allowed-tools: ["Bash(git add:*)", "Bash(git status:*)", "Bash(git diff:*)", "Bash(git commit:*)"]
---

# Commit Flow

1. Review the diff and staged files.
2. Stage the intended changes if needed.
3. Write a conventional commit subject.
4. Run `git commit`.

Keep the message short and subject-only.
