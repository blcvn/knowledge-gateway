---
name: "OPSX: Apply"
description: "Implement tasks from an existing OpenSpec change"
category: Workflow
tags: [workflow, openspec, apply]
---

Use this when the user wants to implement an existing OpenSpec change.

Pick the active change from `openspec/changes/` or use the change name the user provided, read the change artifacts first, and then work through tasks in order.
